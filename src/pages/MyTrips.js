import { ScrollView } from "react-native";
import { VStack } from "native-base";

export default function MyTrips() {


  return(
    <ScrollView>
      <VStack>

      </VStack>
    </ScrollView>
  )
}
